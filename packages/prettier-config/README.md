# `@repo/prettier-config`

Shared Prettier configuration for the FlowForge monorepo.

Consume it by adding to a workspace `package.json`:

```json
{
  "prettier": "@repo/prettier-config",
  "devDependencies": {
    "@repo/prettier-config": "*"
  }
}
```
